name: publish
on:
  push:
    branches:    
      - main
defaults:
  run:
    shell: bash
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Hugo
        run: sudo snap install hugo
      - name: Hugo Version
        run: hugo version